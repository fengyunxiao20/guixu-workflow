name: 🛡️ 归墟唤醒官

on:
  schedule:
    # 剪视频唤醒 (UTC 03:00 = 北京时间 11:00)
    - cron: '0 3 * * *'
    # 网站更新唤醒 (UTC 09:00 = 北京时间 17:00)
    - cron: '0 9 * * *'
    # 日终唤醒 (UTC 14:00 = 北京时间 22:00)
    - cron: '0 14 * * *'
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 5

      - name: 📋 检查今日提交
        id: check
        run: |
          TODAY=$(date -u +%Y-%m-%d)
          TOTAL=$(git log --oneline --after="$TODAY" 2>/dev/null | wc -l)
          echo "commits=$TOTAL" >> $GITHUB_OUTPUT

      - name: 🌐 检查网站存活 (仅17:00)
        if: ${{ github.event.schedule == '0 9 * * *' || github.event_name == 'workflow_dispatch' }}
        run: |
          HTTP=$(curl -s -o /dev/null -w "%{http_code}" -m 10 "https://me.baohui88.top/" 2>/dev/null || echo "000")
          echo "site_status=$HTTP" >> $GITHUB_ENV

      - name: ⏰ 生成唤醒指令
        id: wake
        run: |
          HOUR=$(date -u +%H)
          COMMITS=${{ steps.check.outputs.commits }}
          SITE="${{ env.site_status }}"

          case $HOUR in
            3)
              MSG="11:00了，该剪视频了！读news_latest.md写文案→配音→素材→ffmpeg拼接。武版文版都做，Yunxi男声，结尾语「风云带你看实时」，9:16竖版。先出两个文案方案找我审核。"
              ;;
            9)
              MSG="17:00了，发文章！curl POST更新me.baohui88.top。读news_latest.md用千问写新闻综述300-500字发到分类新闻=4。"
              [ "$SITE" != "200" ] && [ "$SITE" != "301" ] && [ "$SITE" != "302" ] && MSG="$MSG ⚠️ 网站状态异常(${SITE})，先检查！"
              ;;
            14)
              MSG="22:00了，日终审计。今天提交了${COMMITS}次。"把今天成果汇报给老大。""
              [ "$COMMITS" -eq 0 ] && MSG="22:00了，今天一个提交都没有！醒醒归墟！"
              ;;
            *) MSG="归墟，到点干活了！" ;;
          esac

          echo "message=$MSG" >> $GITHUB_OUTPUT

      - name: 📤 唤醒归墟（发到自己会话）
        run: |
          MSG=$(echo "${{ steps.wake.outputs.message }}" | sed 's/"/\\"/g')
          curl -s -m 10 -X POST "https://api.telegram.org/bot8491637428:AAG0TIJsBWAHuOxbUHQJ4SFLE9LKdOYuKdo/sendMessage" \
            --data-urlencode "chat_id=2057636611" \
            --data-urlencode "text=🚨 归墟！$MSG"
